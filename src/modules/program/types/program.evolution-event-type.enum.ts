export enum ProgamEvolutionEventTypeEnum {
  registered = "registered",
  started = "started",
  paused = "paused",
  progressed = "progressed",
  blocked = "blocked",
  canceled = "canceled",
  done = "done",
}
