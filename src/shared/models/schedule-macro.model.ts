export interface ScheduleMacroModel {
  id: number;
  name: string;
  iconPath: string; // Chemin de l'icône
  macroValue: string;
  macroUnit: string;
  iconColor: string; // Couleur de l'icône (en format hexadécimal)
}
